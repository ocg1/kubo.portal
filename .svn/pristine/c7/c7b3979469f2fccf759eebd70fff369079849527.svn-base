console.log("mayusculas.js");

function mayusculas () 
{
	if($("#cveelector").is(":visible")) {
			$("#cveelector").val(($("#cveelector").val()).toUpperCase()); 
	}
	
	console.log("funcion mayusculas");
	
	$(".mayus").blur( function () {
		
	    $(this).val(($(this).val()).toUpperCase());
            if (this.value.match(/[^a-zA-Z0-9 ]/g)) {
                this.value = this.value.replace(/[^a-zA-Z0-9 ]/g, '');
            }
		
	});

}
