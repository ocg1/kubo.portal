package mx.com.kubo.notificaciones.notificacion;


public abstract class DestinatariosAMO extends NotificacionDMO
{
	/*
	 protected final void asignarLista_TO() 
	 {
		lstMailTo  = new ArrayList<String>();
		 
		if(strTo != null && !strTo.isEmpty())
		{
			if(strTo.contains(","))
			{
				String[] lista_receptores_TO = strTo.split(",");
				
				for(String receptor: lista_receptores_TO)
				{
					lstMailTo.add(receptor.trim());
				}
			} else {
				lstMailTo.add(strTo.trim());
			}
		}
	 }
	 
	 protected final void asignarLista_CC()
	 {
		lstMailCC  = new ArrayList<String>();
			
		if(strCC != null && !strCC.isEmpty())
		{
			if(strCC.contains(","))
			{
				String[] lstTo = strCC.split(",");
				
				for(int i = 0; i < lstTo.length; i++)
				{
					lstMailCC.add(lstTo[i].trim());
				}
			} else {
				lstMailCC.add(strCC.trim());
			}
		}
	 }
	 
	 protected final void asignarLista_CCO()
	 {
		lstMailCCO = new ArrayList<String>();
		
		if(strCCO != null && !strCCO.isEmpty())
		{
			if(strCCO.contains(","))
			{
				String[] lstTo = strCCO.split(",");
				
				for(int i = 0; i<lstTo.length;i++)
				{
					lstMailCCO.add(lstTo[i].trim());
				}
			} else {
				lstMailCCO.add(strCCO.trim());
			}
		}
	 }
	 */
}
